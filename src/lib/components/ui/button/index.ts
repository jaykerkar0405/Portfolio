import Root, { buttonVariants, type ButtonSize, type ButtonProps, type ButtonVariant } from './button.svelte';

export {
	Root,
	Root as Button,
	buttonVariants,
	type ButtonSize,
	type ButtonProps,
	type ButtonVariant,
	type ButtonProps as Props
};
